import React from 'react';
import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { Label, Surface } from '../../src/components/Surface';
import { colors } from '../../src/theme';
export default function Connect() { return <View style={styles.page}><ScrollView contentContainerStyle={styles.content}><Label>PEOPLE & SHARING</Label><Text style={styles.title}>Every person needs a private profile.</Text><Text style={styles.sub}>Sharing health information requires clear permission and control for everyone involved.</Text><Surface style={styles.card}><Text style={styles.cardTitle}>Family access is not available yet</Text><Text style={styles.cardBody}>This preview contains one profile only. Separate records, scoped invitations and immediate access revocation must be in place before Nura can share anyone’s health information.</Text></Surface></ScrollView></View>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.bg }, content: { padding: 22, paddingTop: 43, maxWidth: 560, width: '100%', alignSelf: 'center' }, title: { color: colors.text, fontSize: 33, fontWeight: '300', marginTop: 10 }, sub: { color: colors.muted, fontSize: 13, lineHeight: 20, marginTop: 8, marginBottom: 23 }, card: { padding: 17 }, cardTitle: { color: colors.text, fontSize: 16 }, cardBody: { color: colors.muted, fontSize: 12, lineHeight: 19, marginTop: 8 } });
